[pytest]
# Root-level pytest config so `pytest` from the repo root picks up backend tests.
testpaths =
    backend/tests
pythonpath =
    backend
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
    api: API endpoint tests
    database: Database tests
